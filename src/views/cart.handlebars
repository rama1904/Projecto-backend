<h1>Carrito de compras</h1>

{{#if cart.products.length}}
  <ul>
    {{#each cart.products}}
      <li>
        <img src="{{this.product.image}}" alt="{{this.product.name}}" style="width:50px" />
        {{this.product.name}} - Cantidad: {{this.quantity}} - Precio unitario: ${{this.product.price}}
      </li>
    {{/each}}
  </ul>
  <p>Total: ${{calculateTotal cart.products}}</p>
{{else}}
  <p>El carrito está vacío</p>
{{/if}}